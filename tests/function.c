#include <math.h>
//,
int main () {
	sqrt(2);
	#pragma MPC precision(128) rounding(MPC_RNDZZ) {
		int a = 3+4-2/2;
		a = sqrt(9);
		4+4;
		cos(42);
		pow(1+1,1+2);
		(89-54+6)++;
		a--;
		(-(a+6))*5*(4/2);
	}
	int hello;
}
